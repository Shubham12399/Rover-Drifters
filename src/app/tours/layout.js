export const metadata = {
  title: "Explore Tours",
  // description: "The Next feeling of Tour & Travel.",
};

const Tours = ({children}) => {
  return (
    <>
    {children}
    </>
  );
};

export default Tours;
